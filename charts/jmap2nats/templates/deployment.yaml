apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "jmap2nats.fullname" . }}
  labels:
    {{- include "jmap2nats.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "jmap2nats.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include "jmap2nats.configJson" . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "jmap2nats.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "jmap2nats.serviceAccountName" . }}
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: jmap2nats
          image: {{ include "jmap2nats.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: JMAP2NATS_CONFIG
              value: /etc/jmap2nats/config.json
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/jmap2nats/config.json
              subPath: config.json
              readOnly: true
            - name: secrets
              mountPath: /etc/jmap2nats/secrets
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ include "jmap2nats.fullname" . }}
        - name: secrets
          projected:
            defaultMode: 288
            sources:
              - secret:
                  name: {{ include "jmap2nats.jmapSecretName" . | quote }}
                  items:
                    - key: {{ .Values.secrets.jmap.token }}
                      path: token
              {{- $method := include "jmap2nats.natsMethod" . | trim }}
              {{- $name := include "jmap2nats.natsSecretName" . }}
              {{- $nats := .Values.secrets.nats }}
              {{- if eq $method "token" }}
              - secret:
                  name: {{ $name | quote }}
                  items:
                    - key: {{ $nats.token.token }}
                      path: nats.token
              {{- else if eq $method "user" }}
              - secret:
                  name: {{ $name | quote }}
                  items:
                    - key: {{ $nats.user.user }}
                      path: nats.user
                    - key: {{ $nats.user.password }}
                      path: nats.password
              {{- else if eq $method "creds" }}
              - secret:
                  name: {{ $name | quote }}
                  items:
                    - key: {{ $nats.creds.file }}
                      path: nats.creds
              {{- else if eq $method "nkey" }}
              - secret:
                  name: {{ $name | quote }}
                  items:
                    - key: {{ $nats.nkey.seed }}
                      path: nats.nk
              {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
